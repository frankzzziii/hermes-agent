name: web-crawl4ai
version: 1.0.0
description: "Crawl4ai self-hosted web content extraction. Uses web.crawl4ai.url or CRAWL4AI_URL; auth via CRAWL4AI_API_TOKEN (optional for default local deployments)."
author: NousResearch
kind: backend
provides_web_providers:
  - crawl4ai
